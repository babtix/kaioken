The llm_integration module provides a Go client for interacting with multiple LLM providers (OpenRouter, OpenAI, Anthropic, local models, etc.) with features including automatic retries, token budgeting, usage tracking, streaming responses, and provider-specific protocol translation. It abstracts differences in authentication, request/response formats, and capabilities through a unified Client interface.
