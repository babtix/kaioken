The llm_integration module provides a Go client for interacting with various LLM providers (OpenRouter, OpenAI, Anthropic, local models, etc.) with features including automatic retries, token budgeting, usage tracking, cost accounting, and support for both streaming and non-streaming responses. It handles provider-specific differences in authentication, request/response formats, and error conditions through a unified Client interface.
