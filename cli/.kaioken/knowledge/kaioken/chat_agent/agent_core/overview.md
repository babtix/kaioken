The agent_core module implements the main agent loop for a tool-using coding assistant. It manages conversation context (via compaction, pruning, and reminders), executes tools with user approval for repo-changing actions, handles steering and follow-up queues, and supports delegation and budgeting.
