The agent core implements a tool-using coding assistant loop that manages conversations, executes tools (file operations, command execution, knowledge lookup), enforces mode-based permissions, applies contextual reminders, manages context via compaction and pruning, and guards against excessive spending via a budget guard. It includes sub-agent delegation for read-only investigations and todo list management for task tracking.
