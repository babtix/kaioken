The Agent Core module implements the central Agent struct and its Run method, which orchestrates the tool-calling loop for the Kaioken coding assistant. It manages state including queues for steering and follow-up, context window management via pruning and compaction, budget guarding, and event-driven observability.
