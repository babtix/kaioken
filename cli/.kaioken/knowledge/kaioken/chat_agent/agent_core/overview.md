The agent core implements the main loop for a tool-using coding assistant, handling tool execution with approval hooks, context management (compaction, pruning, budgeting), steering/follow-up queues for user interruptions, and event-driven lifecycle tracking. It manages permissions via a ruleset, supports sub-agent delegation for read-only tasks, and tracks file operations for context summarization.
