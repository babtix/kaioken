The module uses the Go standard library, along with golang.org/x/text/unicode/norm for Unicode normalization in edit matching (editmatch.go) and golang.org/x/sys/windows for Windows process-tree control (proc_windows.go). It depends on internal packages: llm, events, config, ext, memory, and gitx.
