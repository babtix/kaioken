The module is written in Go and uses the standard library. It depends on internal packages: llm, memory, ext, skills, and config. The llm.Client interface is used for interactions with language models, but the specific implementation is external to this module. Additionally, editmatch.go depends on golang.org/x/text/unicode/norm for Unicode normalization.
