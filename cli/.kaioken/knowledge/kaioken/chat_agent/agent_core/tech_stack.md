The module uses the Go standard library (context, fmt, strings, sync, os, path/filebuf, etc.), the internal llm package for LLM interactions, and internal packages for events, config, extensions, skills, and memory. It also uses golang.org/x/text/unicode/norm for Unicode normalization in edit matching and golang.org/x/sys/windows for Windows-specific process control.
