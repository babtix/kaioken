The kaioken/tui module provides a terminal user interface for the Kaioken application using the Bubble Tea library. It features an interactive chat interface with slash commands for operations like model interaction, knowledge engine commands (/wiki, /cards), session management, extension handling, and impact prediction (/impact). The TUI renders markdown responses, manages agent approvals, includes a dynamic command palette for autocomplete, and provides an interactive tree view for impact analysis reports.
