- Slash command handlers must be Model methods named do<Command> or start<Command> (e.g., doExt, startResearch) returning (tea.Model, tea.Cmd).\n- The command registry (commands.go) must stay in sync with dispatch; a test ensures every registry entry is accepted by dispatch.\n- New commands must be added to the chapters slice in tutorial.go to appear in the guided tutorial.\n- The explain command requires a corresponding entry in commandGuides (explain.go) for detailed reference.\n- UI rendering uses predefined lipgloss styles (promptStyle, hintStyle, etc.) updated via applyTheme (theme.go).\n- Long-running operations must check guardBusy() and set the busy flag, sending busyMsg and doneMsg via the events channel.\n- The markdownRenderer (markdown.go) caches the glamour renderer by width to avoid rebuilds.\n- The command palette limits visible rows to maxPaletteRows (8) and handles scrolling via an offset.\n- Session commands (/switch, /import) must call m.saveSession() before switching or importing to preserve state.\n- Error messages use errStyle, warnings warnStyle, and success okStyle (defined in tui.go).
