The gitx package provides Git repository interaction by shelling out to the git binary. It offers functions to detect changes (committed, staged, unstaged, untracked), manage post-commit hooks for automatic wiki updates, and perform staging, committing, and diff operations. The package is used to answer the question: 'what changed in this repository since the last documentation run?'
