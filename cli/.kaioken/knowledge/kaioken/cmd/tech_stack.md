The module uses the Go standard library (including context, flag (via custom parser), os/signal, filepath, sync, time) and depends on the project's internal packages (such as internal/config, internal/generate, internal/llm, internal/scan, etc.) for core functionality. No external frameworks or libraries are used beyond the standard library and the project's own internal code.
