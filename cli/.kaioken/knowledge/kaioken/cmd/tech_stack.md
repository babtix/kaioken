The module uses the Go standard library (context, errors, fmt, os, os/signal, runtime, strings, sync, time, bufio) and internal packages (agentsmd, config, daemon, export, generate, gitx, llm, plan, scan, serve, setup, skills, state, tui, version, wiki, ext). No external frameworks or libraries beyond the Go standard library and the project's internal modules are used.
