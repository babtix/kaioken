The module is written in Go using the standard library for flag parsing (custom implementation), context handling, and OS interactions. It relies on the project's internal packages (e.g., internal/config, internal/agent, internal/mcp) for core functionality. No external CLI frameworks or web libraries are used; the command-line interface is built from scratch.
