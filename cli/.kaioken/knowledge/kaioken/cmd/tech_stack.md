The module uses the Go standard library for argument parsing, context handling, and file operations. It depends on internal kaioken packages for configuration (config), LLM integration (llm), repository scanning (scan), module planning (plan), knowledge card generation (generate), wiki operations (wiki), skill building (skills), HTTP serving (serve and daemon), state tracking (state), terminal UI (tui), hook management (hook), git operations (gitx), and version reporting (version).
