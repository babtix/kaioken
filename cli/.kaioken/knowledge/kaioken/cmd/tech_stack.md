The module uses only the Go standard library (flag parsing via custom function, context, os/signal, sync, time, etc.) and depends on internal Kaioken packages (config, scan, plan, generate, etc.). No external frameworks or libraries are directly imported in this module.
