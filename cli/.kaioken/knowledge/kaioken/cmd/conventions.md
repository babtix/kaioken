Command functions must be named cmd<CommandName> (e.g., cmdInit, cmdScan) and return error; Use the flags struct to hold parsed command-line arguments (repo, model, module, base, port, force, full, out, positional, positionals, token, tokenStdin); Parse arguments using parseFlags() which handles -key value pairs and boolean flags; For commands requiring LLM, create client via newClient(cfg, f) after loading config; Handle interrupts via signal.NotifyContext() and defer stop(); Return errors to main() for consistent error reporting (fmt.Fprintln(os.Stderr, "error:", err); os.Exit(1)); For ext subcommands, extract subcommand and argument from f.positionals[0] and f.positionals[1]; Use sync.Map for tracking revised module IDs in generate/update workflows; Progress reporting via callback functions (Info, Started, Wrote, Failed) matching internal package interfaces; Default repo to "." when -repo flag not provided
