The module centers around main() in cmd/kaioken/main.go, which uses a custom flags parser to interpret arguments into a flags struct. It switches on the first argument (command) to call corresponding handler functions (e.g., cmdInit, cmdScan). Handlers typically load configuration via config.Load(), create an LLM client via newClient(), and delegate to internal packages (scan, plan, generate, wiki, skills, serve, daemon, state, tui, hook, gitx) for core functionality. Error handling returns errors to main() for reporting and exit. Long-running operations accept a context for cancellation (set up via signal.NotifyContext). The flags struct standardizes options like -repo, -model, -module, -base, -port, -force, and -token. Progress reporting uses structured callbacks (e.g., wiki.Progress, skills.Progress) for consistent output.
