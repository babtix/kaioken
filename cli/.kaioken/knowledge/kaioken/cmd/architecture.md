The module centers around the main function in cmd/kaioken/main.go, which serves as the program's entry point. After preliminary tasks like cleaning up old binaries from self-update and checking for update notices, it parses the command and arguments using a custom flag parser (parseFlags). A signal context is established for graceful shutdown, and the command is dispatched to a handler function (e.g., cmdInit, cmdRun) based on the first argument. Each handler function, defined in files like main.go, ext.go, impact.go, etc., follows a pattern of loading configuration via config.Load, initializing an LLM client via newClient, performing core operations such as executing the command's logic, and returning any errors.
