The module centers around main.go, which contains the main function responsible for global setup (cleaning up old binaries, checking for updates, handling signals) and dispatching to command-specific functions based on the first argument. Each command is implemented in its own file (e.g., run.go for the run and rpc commands, ext.go for extension management) and follows a pattern of loading configuration, creating an LLM client if needed, executing command logic, and handling errors. The main function also records usage statistics via bookSpend after command execution (but before exiting on error) and maps specific errors (like errStale) to appropriate exit codes. Command functions typically use the flags struct to access parsed command-line arguments, respect the context for cancellation, and output results to stdout or stderr as appropriate.
