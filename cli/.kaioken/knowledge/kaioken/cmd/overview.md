The cmd/kaioken module is the main command-line interface for the Kaioken knowledge engine. It parses user commands and flags, dispatches to internal packages for execution (such as scanning, generating knowledge cards, running the agent, etc.), and handles cross-cutting concerns like update notices, signal handling, and usage tracking.
