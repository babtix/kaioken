The kaioken/cmd module provides the command-line interface for the Kaioken application. It parses user commands and flags, dispatches to command-specific implementations, and handles cross-cutting concerns like update notifications, signal handling, and usage tracking. The main entry point is in main.go, which routes to subcommand functions defined in separate files (e.g., run.go, init.go).
