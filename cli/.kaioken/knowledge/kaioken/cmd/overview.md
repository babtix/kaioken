The kaioken/cmd module provides the main entry point and command-line interface for the Kaioken knowledge engine. It parses command-line arguments, dispatches to subcommands for tasks like initialization, scanning, generation, and serving, and handles cross-cutting concerns such as error reporting, usage tracking, and signal handling.
