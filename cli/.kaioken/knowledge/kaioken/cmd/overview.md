The kaioken/cmd module is the command-line entry point for the Kaioken application. It parses user input, dispatches to command-specific handlers (like init, scan, generate, etc.), and manages overall execution flow including signal handling, error reporting, and cleanup of outdated binaries from self-updates.
