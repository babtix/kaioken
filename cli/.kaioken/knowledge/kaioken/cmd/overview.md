The cmd/kaioken module provides the command-line interface for the Kaioken knowledge engine. It contains the main entry point and implements various subcommands (e.g., run, init, generate, tui) that perform core functions like scanning repositories, generating knowledge cards, and interacting with LLMs.
