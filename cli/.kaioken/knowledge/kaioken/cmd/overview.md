The kaioken/cmd module is the command-line interface entry point for the Kaioken application. It parses user commands and flags, then dispatches to specific command handlers (defined in main.go and ext.go) which interact with internal packages to perform tasks like scanning, planning, generating knowledge cards, managing extensions, and serving the wiki.
