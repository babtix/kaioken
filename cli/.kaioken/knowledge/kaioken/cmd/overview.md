The cmd/kaioken module provides the command-line interface for the Kaioken application. It parses command-line arguments and flags, then dispatches to command-specific handlers implemented in this package or internal packages. The module handles global concerns like update notices, signal handling, usage tracking, and error routing.
