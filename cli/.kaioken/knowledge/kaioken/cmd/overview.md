The kaioken/cmd module contains the main entry point and command-line interface for the kaioken application. It parses command-line arguments, dispatches to command-specific handlers, and manages cross-cutting concerns like configuration loading, LLM client initialization, signal handling, and usage tracking. The module defines the main function and implements all top-level commands (e.g., init, run, generate) as functions in the main package.
