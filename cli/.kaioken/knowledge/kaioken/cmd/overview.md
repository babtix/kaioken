The cmd/kaioken module provides the command-line interface for the Kaioken application. It parses user commands and flags, then dispatches to specific command implementations (such as run, tui, init, generate, etc.) each handled in separate source files. The module manages global concerns like update notifications, signal handling, and usage tracking.
