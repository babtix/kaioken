The package defines a mutable string variable Version initialized to "1.0.0" that can be overridden at build time with the -ldflags "-X kaioken/internal/version.Version=..." flag, and an immutable integer constant ContractVersion set to 4 representing the current API contract. There are no functions or initialization logic; consumers import the package and read version.Version or version.ContractVersion directly.
