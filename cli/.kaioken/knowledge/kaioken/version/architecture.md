The package defines a mutable string variable `Version` that is set at build time via `-ldflags`, and an immutable integer constant `ContractVersion` representing the API contract version. There are no functions or internal state; the values are read directly by importing packages. The module has no dependencies on other internal packages and performs no initialization, authentication, caching, or error handling.
