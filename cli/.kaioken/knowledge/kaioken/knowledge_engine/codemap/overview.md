The codemap module extracts structural skeletons from source files to enable efficient context assembly for AI models. It parses declarations (functions, classes, variables, etc.) with line anchors, allowing token budgets to be split between structural overviews and relevant source bodies. The module supports multiple languages via language-specific parsers and a fallback C-like parser.
