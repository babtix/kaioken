The wiki_serve module provides an HTTP server that serves a generated wiki as a browsable local site. It renders markdown documents, offers search functionality, and includes an interactive graph view of the wiki's structure. The server is designed to run locally without external dependencies, using embedded assets for the graph engine.
