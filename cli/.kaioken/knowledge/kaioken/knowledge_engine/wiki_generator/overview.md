The wiki_generator module generates comprehensive wiki documentation for a codebase using a multi-pass pipeline. It creates a shared architecture brief, plans sections and subsections, generates documents with optional quality passes, and supports incremental updates based on git changes.
