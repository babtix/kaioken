The kaioken/config module manages application configuration for both per-repository and per-user settings. It defines structures for repository-specific configuration (stored in .kaioken/config.yaml) and global user configuration (stored in ~/.kaioken/config.yaml), providing functions to load, save, and manipulate these configurations with default values and validation.
