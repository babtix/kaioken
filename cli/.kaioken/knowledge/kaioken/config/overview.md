The config package manages application configuration for Kaioken, handling both per-repository settings (stored in .kaioken/config.yaml) and global user settings (stored in ~/.kaioken/config.yaml or $KAIOKEN_HOME/config.yaml). It provides functions to load, validate, and save these configurations, including defaults for LLM provider, model, concurrency, token limits, scope, memory, and budget.
