The config package manages application configuration for Kaioken, handling both repository-specific settings (.kaioken/config.yaml) and user-global settings (~/.kaioken/config.yaml). It provides functions to load, save, and derive configuration values including defaults, concurrency limits for free models, and memory/learning thresholds.
