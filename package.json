{
  "name": "kaioken",
  "version": "2.0.0",
  "private": true,
  "description": "The repository that knows your code",
  "scripts": {
    "build": "npm --prefix website run build && node -e \"require('fs').cpSync('website/dist', 'dist', {recursive: true, force: true})\"",
    "install": "npm --prefix website install",
    "desktop:install": "npm --prefix desktop install",
    "desktop:dev": "npm --prefix desktop run dev",
    "desktop:build": "npm --prefix desktop run build"
  }
}
