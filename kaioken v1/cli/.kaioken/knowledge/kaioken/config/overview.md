The kaioken/config module manages application configuration for the Kaioken tool, handling both per-repository settings (stored in .kaioken/config.yaml) and global user settings (stored in ~/.kaioken/config.yaml). It provides functions to load, save, and resolve configuration values, including model selection, concurrency limits, and feature toggles for memory, search, and budgeting.
