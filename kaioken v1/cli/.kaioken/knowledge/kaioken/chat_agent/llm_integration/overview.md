The llm package provides a client for interacting with various LLM providers (OpenRouter, OpenAI, Anthropic, etc.) with support for retries, token budgeting, usage tracking, and provider-specific translations (especially for Anthropic's Messages API). It includes a registry of providers, handles authentication via API keys or local endpoints, and manages streaming and non-streaming chat completions.
